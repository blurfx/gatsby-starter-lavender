import { styled } from '~/stitches.config';

export const Container = styled('footer', {
  marginTop: 'auto',
  paddingTop: '2rem',
  paddingBottom: '1.5rem',

  color: '$text100',
  fontSize: '0.875rem',
  textAlign: 'center',
});
